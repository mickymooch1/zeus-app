fastapi>=0.115.0
uvicorn>=0.30.0
websockets>=12.0
anyio>=4.0.0
httpx>=0.27.0
anthropic==1.3.0  # pinned: was >=0.40.0, silently drifted to 1.3.0 which dropped Messages.create()'s
                  # temperature/top_p/top_k kwargs entirely (TypeError, not a 400) — took down ALL
                  # song generation, not just kids/story mode, with no alert (2026-09). Same lesson
                  # as the stripe pin below: an unbounded floating minimum let a breaking major
                  # version reach production unnoticed. Bump deliberately, not by omission.
pytest>=8.0.0
pytest-asyncio>=0.23.0
PyJWT>=2.8.0
passlib[bcrypt]>=1.7.4
bcrypt>=3.0.0,<4.0.0
stripe>=15,<16  # pinned: 15.x changed StripeObject (.get() breaks); unpinned drift silently broke billing (2026-07)
fpdf2>=2.7.0
python-docx>=1.1.0
apscheduler>=3.10.0,<4.0.0
croniter>=2.0.0
google-auth-oauthlib>=1.2.0
google-api-python-client>=2.120.0
python-multipart>=0.0.9
requests>=2.31.0
Pillow>=10.0.0
pillow-heif>=0.18.0
slowapi>=0.1.9
python-telegram-bot>=21.0

